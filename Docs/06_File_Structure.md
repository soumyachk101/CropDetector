# 📁 File Structure
## Crop Detector — Monorepo Directory Layout & Code Organization

**Version:** 1.0.0  
**Date:** June 2026

---

## 1. Monorepo Overview

```
crop-detector/
├── apps/
│   ├── web/                  # Next.js 14 frontend (PWA)
│   └── api/                  # Express.js backend
├── services/
│   └── ai-engine/            # FastAPI AI inference service
├── packages/
│   ├── types/                # Shared TypeScript types
│   ├── config/               # Shared ESLint, Prettier, TS config
│   └── ui/                   # Shared UI component library
├── prisma/                   # Database schema & migrations
├── scripts/                  # Dev/deploy utility scripts
├── docs/                     # This documentation
├── .github/
│   └── workflows/            # CI/CD pipelines
├── docker-compose.yml        # Local dev environment
├── turbo.json                # Turborepo config
├── package.json              # Root workspace
└── README.md
```

---

## 2. Frontend — `apps/web/`

```
apps/web/
├── public/
│   ├── icons/                # PWA icons (48px to 512px)
│   ├── images/               # Static images, og-image
│   ├── locales/              # i18n JSON files
│   │   ├── en.json
│   │   ├── hi.json
│   │   ├── bn.json
│   │   ├── te.json
│   │   ├── ta.json
│   │   ├── mr.json
│   │   └── kn.json
│   ├── manifest.json         # PWA manifest
│   └── sw.js                 # Service worker (generated by next-pwa)
│
├── src/
│   ├── app/                  # Next.js App Router
│   │   ├── (auth)/
│   │   │   ├── login/
│   │   │   │   └── page.tsx
│   │   │   ├── register/
│   │   │   │   └── page.tsx
│   │   │   └── layout.tsx
│   │   │
│   │   ├── (main)/
│   │   │   ├── layout.tsx    # Main app layout with bottom nav
│   │   │   ├── page.tsx      # Home screen
│   │   │   ├── scan/
│   │   │   │   ├── page.tsx          # Camera / upload screen
│   │   │   │   ├── processing/
│   │   │   │   │   └── page.tsx      # AI processing screen
│   │   │   │   └── result/
│   │   │   │       └── [scanId]/
│   │   │   │           └── page.tsx  # Detection result
│   │   │   ├── report/
│   │   │   │   └── [scanId]/
│   │   │   │       └── page.tsx      # Full disease report
│   │   │   ├── history/
│   │   │   │   └── page.tsx
│   │   │   ├── dashboard/
│   │   │   │   └── page.tsx
│   │   │   └── profile/
│   │   │       └── page.tsx
│   │   │
│   │   ├── share/
│   │   │   └── [token]/
│   │   │       └── page.tsx          # Public shareable report
│   │   │
│   │   ├── api/              # Next.js Route Handlers (BFF layer)
│   │   │   ├── auth/
│   │   │   │   └── [...nextauth]/
│   │   │   │       └── route.ts
│   │   │   └── upload/
│   │   │       └── route.ts          # Proxy file upload
│   │   │
│   │   ├── globals.css
│   │   ├── layout.tsx        # Root layout (fonts, providers)
│   │   └── not-found.tsx
│   │
│   ├── components/
│   │   ├── ui/               # shadcn/ui base components
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   ├── badge.tsx
│   │   │   ├── sheet.tsx
│   │   │   ├── accordion.tsx
│   │   │   └── ...
│   │   │
│   │   ├── layout/
│   │   │   ├── BottomNav.tsx
│   │   │   ├── TopHeader.tsx
│   │   │   └── PageWrapper.tsx
│   │   │
│   │   ├── scan/
│   │   │   ├── CameraView.tsx        # Camera capture component
│   │   │   ├── ImageUploader.tsx     # Gallery upload + preview
│   │   │   ├── QualityMeter.tsx      # Real-time blur/light indicator
│   │   │   └── ScanProcessing.tsx    # Animated processing screen
│   │   │
│   │   ├── result/
│   │   │   ├── DetectionCard.tsx     # Healthy / Diseased result card
│   │   │   ├── HeatmapViewer.tsx     # GradCAM overlay viewer
│   │   │   ├── SeverityBadge.tsx     # Mild/Moderate/Severe badge
│   │   │   └── ConfidenceBar.tsx     # Confidence percentage bar
│   │   │
│   │   ├── report/
│   │   │   ├── ReportHeader.tsx
│   │   │   ├── TreatmentSection.tsx  # Organic + chemical accordion
│   │   │   ├── SymptomChecklist.tsx
│   │   │   ├── ProgressionTimeline.tsx
│   │   │   ├── ExpertDisclaimer.tsx
│   │   │   └── ShareReportModal.tsx
│   │   │
│   │   ├── dashboard/
│   │   │   ├── ScanHistoryList.tsx
│   │   │   ├── HealthTrendChart.tsx
│   │   │   ├── DiseaseDistributionPie.tsx
│   │   │   └── FieldCard.tsx
│   │   │
│   │   └── common/
│   │       ├── WeatherWidget.tsx
│   │       ├── LoadingSpinner.tsx
│   │       ├── EmptyState.tsx
│   │       ├── ErrorBoundary.tsx
│   │       ├── OfflineBanner.tsx
│   │       └── LanguageSwitcher.tsx
│   │
│   ├── hooks/
│   │   ├── useCamera.ts              # Camera access + capture logic
│   │   ├── useScan.ts                # Scan upload + polling
│   │   ├── useReport.ts              # Report fetch + cache
│   │   ├── useHistory.ts             # Paginated scan history
│   │   ├── useOffline.ts             # Network status + offline queue
│   │   ├── useWeather.ts             # Weather data fetch
│   │   └── useAuth.ts                # Auth state
│   │
│   ├── store/
│   │   ├── authStore.ts              # Zustand: user auth state
│   │   ├── scanStore.ts              # Zustand: active scan state
│   │   └── settingsStore.ts          # Zustand: language, prefs
│   │
│   ├── lib/
│   │   ├── api.ts                    # Axios instance + interceptors
│   │   ├── queryClient.ts            # React Query client config
│   │   ├── utils.ts                  # General helpers
│   │   ├── imageUtils.ts             # Image compression, validation
│   │   └── pdfExport.ts              # Client-side PDF generation
│   │
│   ├── types/
│   │   ├── scan.ts
│   │   ├── report.ts
│   │   ├── user.ts
│   │   └── api.ts
│   │
│   └── config/
│       ├── constants.ts
│       └── routes.ts
│
├── next.config.js
├── tailwind.config.ts
├── tsconfig.json
└── package.json
```

---

## 3. Backend API — `apps/api/`

```
apps/api/
├── src/
│   ├── index.ts              # Express app entry point
│   ├── server.ts             # Server bootstrap + middleware
│   │
│   ├── routes/
│   │   ├── index.ts          # Route aggregator
│   │   ├── auth.routes.ts
│   │   ├── scan.routes.ts
│   │   ├── report.routes.ts
│   │   └── user.routes.ts
│   │
│   ├── controllers/
│   │   ├── auth.controller.ts
│   │   ├── scan.controller.ts
│   │   ├── report.controller.ts
│   │   └── user.controller.ts
│   │
│   ├── services/
│   │   ├── auth.service.ts           # JWT, OAuth, password logic
│   │   ├── scan.service.ts           # Upload, queue dispatch
│   │   ├── report.service.ts         # Report fetch, PDF gen, share
│   │   ├── ai.service.ts             # HTTP calls to FastAPI AI engine
│   │   ├── storage.service.ts        # S3 upload/delete/signed URLs
│   │   ├── notification.service.ts   # Push + in-app notifications
│   │   └── weather.service.ts        # OpenWeatherMap integration
│   │
│   ├── workers/
│   │   ├── scanWorker.ts             # BullMQ: process scan jobs
│   │   └── cleanupWorker.ts          # BullMQ: delete old images
│   │
│   ├── middleware/
│   │   ├── auth.middleware.ts        # JWT verify
│   │   ├── rateLimit.middleware.ts   # Per-IP + per-user limits
│   │   ├── upload.middleware.ts      # Multer config + file validation
│   │   ├── error.middleware.ts       # Global error handler
│   │   └── logger.middleware.ts      # Morgan + custom logger
│   │
│   ├── validators/
│   │   ├── auth.validator.ts         # Zod schemas for auth endpoints
│   │   ├── scan.validator.ts
│   │   └── user.validator.ts
│   │
│   ├── utils/
│   │   ├── jwt.ts
│   │   ├── bcrypt.ts
│   │   ├── s3.ts
│   │   ├── redis.ts
│   │   └── logger.ts
│   │
│   └── config/
│       ├── env.ts                    # Zod-validated env vars
│       ├── redis.config.ts
│       └── bull.config.ts
│
├── prisma/                           # Symlinked from root /prisma
├── tsconfig.json
└── package.json
```

---

## 4. AI Engine — `services/ai-engine/`

```
services/ai-engine/
├── app/
│   ├── main.py               # FastAPI app entry
│   ├── routers/
│   │   └── predict.py        # POST /predict endpoint
│   │
│   ├── core/
│   │   ├── inference.py      # ONNX model loading + inference
│   │   ├── preprocess.py     # Image preprocessing pipeline
│   │   ├── gradcam.py        # GradCAM heatmap generation
│   │   ├── quality_check.py  # Blur + resolution checks
│   │   └── postprocess.py    # Result formatting + thresholding
│   │
│   ├── models/
│   │   ├── efficientnetv2_b3.onnx    # Main inference model
│   │   ├── mobilenetv3_lite.tflite   # Offline / edge model
│   │   └── class_labels.json         # 38 class index → label map
│   │
│   ├── schemas/
│   │   ├── request.py        # Pydantic input schema
│   │   └── response.py       # Pydantic output schema
│   │
│   └── utils/
│       ├── image_utils.py
│       └── logger.py
│
├── tests/
│   ├── test_inference.py
│   ├── test_preprocess.py
│   └── test_quality_check.py
│
├── requirements.txt
├── Dockerfile
└── README.md
```

---

## 5. Prisma — Root `/prisma/`

```
prisma/
├── schema.prisma             # Full data model
├── migrations/               # Auto-generated migration history
│   └── 20260601_init/
│       └── migration.sql
└── seed/
    ├── index.ts              # Seed entry
    └── data/
        ├── diseases.json     # 38+ disease KB entries
        └── crops.json        # Supported crop types
```

---

## 6. Shared Packages

```
packages/
├── types/
│   ├── src/
│   │   ├── scan.ts
│   │   ├── report.ts
│   │   ├── user.ts
│   │   └── index.ts
│   ├── tsconfig.json
│   └── package.json
│
├── config/
│   ├── eslint-preset.js
│   ├── prettier.config.js
│   ├── tsconfig.base.json
│   └── package.json
│
└── ui/                       # Optional: shared React component lib
    ├── src/
    │   └── components/
    ├── tsconfig.json
    └── package.json
```

---

## 7. CI/CD — `.github/workflows/`

```
.github/workflows/
├── ci.yml                    # Run on PR: lint + test + build
├── deploy-staging.yml        # On merge to develop → deploy staging
└── deploy-prod.yml           # On merge to main → deploy production
```

---

## 8. Environment Variables

### `apps/api/.env`
```env
# Server
NODE_ENV=production
PORT=4000

# Database
DATABASE_URL=postgresql://user:pass@host:5432/cropdetector

# Redis
REDIS_URL=redis://host:6379

# JWT
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=30d

# AWS
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1
S3_BUCKET_NAME=cropdetector-images
CLOUDFRONT_BASE_URL=https://cdn.cropdetector.app

# AI Engine
AI_ENGINE_URL=http://ai-engine:8000

# OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# External APIs
OPENWEATHER_API_KEY=

# Anthropic (for report generation)
ANTHROPIC_API_KEY=
```

### `services/ai-engine/.env`
```env
MODEL_PATH=app/models/efficientnetv2_b3.onnx
LITE_MODEL_PATH=app/models/mobilenetv3_lite.tflite
LABELS_PATH=app/models/class_labels.json
MAX_IMAGE_SIZE_MB=10
MIN_RESOLUTION=224
BLUR_THRESHOLD=80
LOG_LEVEL=info
```
